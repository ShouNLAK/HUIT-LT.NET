<UserControl x:Class="_38_NguyenLeAnhKhoa_Tuan07.Bai_2.View.UC_DanhSachHoaDon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Danh sách hóa đơn" FontSize="22" FontWeight="Bold" Margin="10,5,0,10"/>

        <Border Grid.Row="1" Margin="5" Padding="10" BorderBrush="#FFDDDDDD" BorderThickness="1" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Từ ngày"/>
                    <DatePicker SelectedDate="{Binding TuNgay}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Đến ngày"/>
                    <DatePicker SelectedDate="{Binding DenNgay}"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Lọc" Margin="10,18,0,0" Height="30" Command="{Binding LocHoaDonCommand}"/>
                <Button Grid.Column="3" Content="Tất cả" Margin="10,18,0,0" Height="30" Command="{Binding HienThiTatCaCommand}"/>

                <StackPanel Grid.Column="4" HorizontalAlignment="Right">
                    <TextBlock Text="Thống kê nhanh" FontWeight="Bold" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding TongSoHoaDonText, StringFormat=Tổng số hóa đơn: {0}}" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding TongDoanhThuText, StringFormat=Tổng doanh thu: {0}}" HorizontalAlignment="Right" Foreground="#FF1E5DE6" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" Margin="5" ItemsSource="{Binding DanhSachHoaDonHienThi}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã HD" Binding="{Binding MaHoaDon}" Width="120"/>
                <DataGridTextColumn Header="Ngày lập" Binding="{Binding NgayLap, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                <DataGridTextColumn Header="Khách" Binding="{Binding ThongTinKhach.TenKhachHang}" Width="*"/>
                <DataGridTextColumn Header="SĐT" Binding="{Binding ThongTinKhach.SoDienThoai}" Width="120"/>
                <DataGridTextColumn Header="Bàn" Binding="{Binding Ban}" Width="80"/>
                <DataGridTextColumn Header="Tạm tính" Binding="{Binding TongTamTinh}" Width="110"/>
                <DataGridTextColumn Header="Giảm giá" Binding="{Binding GiamGia}" Width="110"/>
                <DataGridTextColumn Header="Thanh toán" Binding="{Binding ThanhToan}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Margin="5" Padding="10" BorderBrush="#FFDDDDDD" BorderThickness="1" CornerRadius="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Tổng số hóa đơn: " FontWeight="Bold"/>
                <TextBlock Text="{Binding TongSoHoaDonText}" Margin="4,0,20,0" FontWeight="Bold"/>
                <TextBlock Text="Tổng tiền thanh toán: " FontWeight="Bold"/>
                <TextBlock Text="{Binding TongDoanhThuText}" FontWeight="Bold" Foreground="#FF1E5DE6"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
