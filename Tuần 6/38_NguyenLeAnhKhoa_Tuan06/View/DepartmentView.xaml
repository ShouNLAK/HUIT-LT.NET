<Window x:Class="_38_NguyenLeAnhKhoa_Tuan06.View.DepartmentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_38_NguyenLeAnhKhoa_Tuan06.View"
        xmlns:vm="clr-namespace:_38_NguyenLeAnhKhoa_Tuan06.ViewModel"
        mc:Ignorable="d"
        Title="Tuần 6 - Quản lý phòng ban MVVM" Height="450" Width="1000">
    <Window.DataContext>
        <vm:PBNVViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="143*"/>
            <ColumnDefinition Width="257*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="352.777"/>
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="10,10,10,10" Margin="10,10,10,10" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="282*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Quản lý nhân viên theo phòng ban (MVVM)" HorizontalAlignment="Left" Margin="10,10,0,10" VerticalAlignment="Top" FontSize="22" FontWeight="Bold"/>
                <Border BorderBrush="Transparent" BorderThickness="0" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="6">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Border Background="#28a745" CornerRadius="6" Padding="8" Margin="6">
                            <TextBlock Foreground="White" FontWeight="Bold" Text="{Binding TotalEmployeeCount}" />
                        </Border>
                        <StackPanel VerticalAlignment="Center" Margin="6">
                            <TextBlock Text="Tổng nhân viên"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1" CornerRadius="10,10,10,10" Margin="20,20,20,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1" CornerRadius="10,10,10,10" Margin="10,10,10,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <Label Content="Quản lý phòng ban" VerticalAlignment="Center" FontWeight="Bold"/>
                        <WrapPanel Grid.Row="1" Margin="10,0,0,0">
                            <TextBox TextWrapping="Wrap" Text="{Binding NewDepartmentName, UpdateSourceTrigger=PropertyChanged}" Width="200" Height="27"/>
                            <Button Content="Thêm" Width="70" Command="{Binding AddDepartmentCommand}"/>
                        </WrapPanel>
                        <Button Content="Xóa phòng ban" HorizontalAlignment="Center" Grid.Row="2" VerticalAlignment="Center" Height="28" Width="294" Margin="10,0,10,0" Command="{Binding RemoveDepartmentCommand}"/>
                    </Grid>
                </Border>
                <TreeView x:Name="tvDepartments" ItemsSource="{Binding Departments}" SelectedItemChanged="tvDepartments_SelectedItemChanged" BorderThickness="0" Background="Transparent">
                    <TreeView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Ten}" Padding="4" />
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
        </Border>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="198*"/>
                <RowDefinition Height="155*"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="10,10,10,10" Margin="10,10,10,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="97*"/>
                        <ColumnDefinition Width="149*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="41*"/>
                        <RowDefinition Height="135*"/>
                    </Grid.RowDefinitions>
                    <Label Content="Danh sách nhân viên" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
                    <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1" HorizontalAlignment="Center" Padding="6" CornerRadius="10,10,10,10">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <StackPanel VerticalAlignment="Center" Margin="6">
                                <TextBlock Text="Số lượng nhân viên"/>
                            </StackPanel>
                            <Border Background="#28a745" CornerRadius="6" Padding="4" Height="31">
                                <TextBlock Foreground="White" Text="{Binding EmployeeCountInSelectedDepartment}" />
                            </Border>
                        </StackPanel>
                    </Border>
                    <DataGrid Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding SelectDepartment.Employees}" SelectedItem="{Binding SelectedEmployee, Mode=TwoWay}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã nhân viên" Width="1*" Binding="{Binding IDNhanVien}"/>
                            <DataGridTextColumn Header="Họ tên" Width="2*" Binding="{Binding NameNhanVien}"/>
                            <DataGridTextColumn Header="Địa chỉ" Width="2*" Binding="{Binding DiaChi}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1" Margin="10,10,10,10" CornerRadius="10,10,10,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Mã nhân viên :" Margin="5,5,5,5"/>
                        <TextBox Text="{Binding EmployeeIDInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150" IsEnabled="{Binding IsEmployeeIdEnabled}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Column="1">
                        <Label Content="Phòng ban :" Margin="5,5,5,5"/>
                        <ComboBox Width="150" ItemsSource="{Binding Departments}" DisplayMemberPath="Ten" SelectedItem="{Binding SelectedDepartmentInForm, Mode=TwoWay}" IsEnabled="{Binding IsInputEnabled}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Row="1">
                        <Label Content="Họ tên :" Margin="5,5,5,5"/>
                        <TextBox Text="{Binding FullNameInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="36,0,0,0" IsEnabled="{Binding IsInputEnabled}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1">
                        <Label Content="Địa chỉ :" Margin="5,5,5,5"/>
                        <TextBox Text="{Binding AddressInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="21,0,0,0" IsEnabled="{Binding IsInputEnabled}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Grid.Row="0" Grid.RowSpan="3" Orientation="Vertical">
                        <Label Content="Thao tác"/>
                        <Button Content="{Binding AddButtonText}" Width="90" Height="30" Command="{Binding AddCommand}"/>
                        <Button Content="{Binding EditButtonText}" Width="90" Height="30" Command="{Binding EditCommand}"/>
                        <Button Content="Lưu" Width="90" Height="30" Command="{Binding SaveCommand}"/>
                        <Button Content="Xóa" Width="90" Height="30" Command="{Binding DeleteCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Window>
