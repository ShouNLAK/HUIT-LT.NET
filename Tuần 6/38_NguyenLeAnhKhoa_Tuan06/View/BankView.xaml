<Window x:Class="_38_NguyenLeAnhKhoa_Tuan06.View.BankView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:_38_NguyenLeAnhKhoa_Tuan06.ViewModel"
        xmlns:local="clr-namespace:_38_NguyenLeAnhKhoa_Tuan06.View"
        mc:Ignorable="d"
        Title="BankView" Height="450" Width="1000">

    <Window.DataContext>
        <vm:BankViewModel/>
    </Window.DataContext>

    <Grid Background="#FFEAEAEA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border BorderThickness="1" Background="#FF252D44">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260*"/>
                    <ColumnDefinition Width="139*"/>
                </Grid.ColumnDefinitions>
                <Label Content="QUẢN LÝ TÀI KHOẢN NGÂN HÀNG" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" FontSize="24" Margin="10,10,0,10" Height="41" Width="408"/>
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#FFE6FF00" FontWeight="Bold" Width="160" Margin="183,0,0,0" Grid.Column="1" Height="20" Text="{Binding TongTien,Mode=OneWay}" />
            </Grid>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="61*"/>
                <ColumnDefinition Width="99*"/>
            </Grid.ColumnDefinitions>
            <Border BorderBrush="Black" BorderThickness="1,1,1,1" CornerRadius="10,10,10,10" Margin="20,20,20,20">
                <Grid Background="White">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <Label Content="Thông tin tài khoản" HorizontalAlignment="Left" Margin="10,10,0,10" VerticalAlignment="Center" Background="{x:Null}" Foreground="#FF252D44" FontWeight="Bold" FontSize="18"/>
                    <WrapPanel Grid.Row="1" Orientation="Vertical">
                        <Label Content="Số tài khoản :"/>
                        <TextBox TextWrapping="Wrap" Text="{Binding SelectedAccount.SoTK, Mode=TwoWay}" Width="300" HorizontalAlignment="Center" Margin="10,0,0,0"/>
                    </WrapPanel>
                    <WrapPanel Grid.Row="2" Orientation="Vertical">
                        <Label Content="Tên khách hàng :"/>
                        <TextBox TextWrapping="Wrap" Text="{Binding SelectedAccount.TenKH, Mode=TwoWay}" Width="300" HorizontalAlignment="Center" Margin="10,0,0,0"/>
                    </WrapPanel>
                    <WrapPanel Grid.Row="3" Orientation="Vertical">
                        <Label Content="Địa chỉ :"/>
                        <TextBox TextWrapping="Wrap" Text="{Binding SelectedAccount.Diachi, Mode=TwoWay}" Width="300" HorizontalAlignment="Center" Margin="10,0,0,0"/>
                    </WrapPanel>
                    <WrapPanel Grid.Row="4" Orientation="Vertical">
                        <Label Content="Thành phố :"/>
                        <ComboBox Width="300" HorizontalAlignment="Center" Margin="10,0,0,0" ItemsSource="{Binding DSTP}" SelectedItem="{Binding SelectedAccount.TP, Mode=TwoWay}"/>
                    </WrapPanel>
                    <WrapPanel Grid.Row="5" Orientation="Vertical">
                        <Label Content="Số tiền :"/>
                        <TextBox TextWrapping="Wrap" Text="{Binding SelectedAccount.SoTien, Mode=TwoWay}" Width="300" HorizontalAlignment="Center" Margin="10,0,0,0"/>
                    </WrapPanel>
                    <WrapPanel Grid.Row="6" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Button Content="{Binding AddButtonText}" Command="{Binding AddCommand}" Margin="10,0,10,0" Width="50" Height="20" Background="#FF00FFF9" Foreground="White"/>
                        <Button Content="Lưu" Command="{Binding SaveCommand}" Margin="10,0,10,0" Width="50" Height="20" Background="#FF00FF1B" Foreground="White"/>
                        <Button Content="{Binding EditButtonText}" Command="{Binding EditCommand}" Margin="10,0,10,0" Width="50" Height="20" Background="#FFFFD200" Foreground="White"/>

                    </WrapPanel>
                </Grid>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1" CornerRadius="10,10,10,10" Margin="20,20,20,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="51*"/>
                        <RowDefinition Height="278*"/>
                    </Grid.RowDefinitions>
                    <Grid Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="286*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Danh sách tài khoản" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center" Background="{x:Null}" Foreground="#FF252D44" FontWeight="Bold" FontSize="18"/>
                        <Button Grid.Column="1" Content="Xóa" HorizontalAlignment="Left" Margin="10,0,10,0" Width="50" Height="20" Background="Red" Foreground="White"/>
                    </Grid>
                    <DataGrid Grid.Row="1" Background="White" AutoGenerateColumns="False" ItemsSource="{Binding DSAccount}" SelectedItem="{Binding SelectedAccount}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="STT" Width="1*" Binding="{Binding STT}"/>
                            <DataGridTextColumn Header="Số TK" Width="2*" Binding="{Binding SoTK}"/>
                            <DataGridTextColumn Header="Khách hàng" Width="2*" Binding="{Binding TenKH}"/>
                            <DataGridTextColumn Header="Địa chỉ" Width="3*" Binding="{Binding Diachi}"/>
                            <DataGridTextColumn Header="TP" Width="2*" Binding="{Binding TP}"/>
                            <DataGridTextColumn Header="Số tiền" Width="2*" Binding="{Binding SoTien}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </Border>
        </Grid>

    </Grid>
</Window>
